query GetPosts {
	posts {
		id
		title
		content
		author {
			id
			name
			email
		}
		createdAt
	}
}

query GetPost($id: ID!) {
	post(id: $id) {
		id
		title
		content
		author {
			id
			name
			email
		}
		createdAt
	}
}
